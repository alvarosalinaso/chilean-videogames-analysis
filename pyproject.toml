[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chilean_videogames"
version = "0.1.0"
authors = [
  { name = "Alvaro Salinas Ortiz", email = "alvaro@example.com" },
]
description = "Análisis de videojuegos desarrollados en Chile"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
]
dependencies = [
    "plotly>=5.19.0",
    "pandas>=2.1.0",
    "numpy>=1.26.0",
    "scipy>=1.12.0",
    "matplotlib>=3.8.0",
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "great_tables>=0.10.0",
    "altair>=5.3.0",
    "seaborn>=0.13.0",
    "networkx>=3.2",
    "scikit-learn>=1.4.0",
    "statsmodels>=0.14.0",
    "dash>=2.14.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.2",
]

[project.urls]
"Homepage" = "https://github.com/alvarosalinaso/chilean-videogames-analysis"
"Bug Tracker" = "https://github.com/alvarosalinaso/chilean-videogames-analysis/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# generate_report.py: trailing double-spaces inside REPORT_TEMPLATE are
# intentional Markdown hard-breaks, not stray whitespace.
"src/chilean_videogames/generate_report.py" = ["W291"]

[tool.pytest.ini_options]
minversion = "7.0"
# Gate = baseline medido (48% core 2026-09-21). Subir solo con tests reales nuevos,
# nunca bajando el gate para "pasar".
addopts = "-ra -q --cov=chilean_videogames --cov-report=term-missing --cov-report=xml --cov-fail-under=45"
testpaths = ["tests"]

[tool.coverage.run]
source = ["src"]
# Omitido: scrapers de red y scripts de visualización/reporte sin lógica testeable
# a nivel unitario (se verifican vía smoke tests + revisión visual).
omit = [
    "src/chilean_videogames/collect.py",
    "src/chilean_videogames/collect_itch.py",
    "src/chilean_videogames/export_visualizations.py",
    "src/chilean_videogames/generate_report.py",
]
branch = true